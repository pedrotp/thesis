var routes = [
  {
    path: '/habits',
    get: function (req, res) {
      // query db for user's habits
      // on success
        // res.json(data);
      // on Error
        // console.error(err); (should error be sent back to front?)
        // res.statusCode(400); 500?
        // res.send(error message viewed by client);
    }
  }
];
